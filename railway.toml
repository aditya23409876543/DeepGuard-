[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "bash -c 'cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 60'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
